import { FilterImagesBySearchStringPipe } from './filter-images-by-search-string.pipe';

describe('FilterImagesBySearchStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterImagesBySearchStringPipe();
    expect(pipe).toBeTruthy();
  });
});
